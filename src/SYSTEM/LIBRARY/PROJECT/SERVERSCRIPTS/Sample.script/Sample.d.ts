declare var base: {
  // Actual parameters

  /** The test parameter */
  test: string;

  // Added by builder
  nodeid: string;
  scriptPriority: number;
  trigger: { name: 'timer'; type: 'timer' };
};
